const initTableConfig = {
  width: 4,
  height: 4,
  cellSize: 50,
};

const initTableInteractive = {
    active: false,
    cellIndex: 0,
    rowIndex: 0,
    offsetLeft: 0,
    offsetTop: 0,
}

export { initTableConfig, initTableInteractive };
